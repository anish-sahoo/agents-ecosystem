import type { createOpencodeClient } from "@opencode-ai/sdk"

/** opencode client type — replace with your runtime's client when adapting. */
export type OpencodeClient = ReturnType<typeof createOpencodeClient>
